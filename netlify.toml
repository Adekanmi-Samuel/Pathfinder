[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Redirects for Next.js
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
